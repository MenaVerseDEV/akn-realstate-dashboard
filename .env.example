# Real API base URL (Heroku)
NEXT_PUBLIC_API_URL=https://akn-api-74a241efe47e.herokuapp.com/api/v1

# CMS mock mode — keep true until CMS endpoints are wired to the real API
NEXT_PUBLIC_USE_MOCK=true
